class ServiceItemModel{

  String title;
  String description;
  String image;
  double distance;
  double latitude;
  double longitude;

  ServiceItemModel({required this.title,
    required this.description,
    required this.image,
    required this.distance,
    required this.longitude,
    required this.latitude
  });
}